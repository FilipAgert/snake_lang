fn f(int x, bool y, z) : bool {
    int d = 4;
    g + x;

    if y == true{
        d = d + 4;
    } else {
        d = d - 2;
    }
    return y;
}

{
    int a = 5;
    int b = 4
    x = 4;
    int c = d - (x + 4;
    int d = f(c, b, c);

    bool eft = true;
    bool bla = false;
    bool res = eft && bla;
    int hej = eft * (c*a);

    {
        bool a = true;
        eft = a && bla;
    }
}