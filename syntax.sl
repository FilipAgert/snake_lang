fn f(int x, bool y, int z) : bool {
    int d = 4;
    int g = x;

    if y == true{
        d = d + 4;
    } else {
        d = d - 2;
    }
    return y;

    return d;
}

{
    bool bla = 1;
    int a = f(bla, 2);

    a = f(2, true, 3, true, true)
}